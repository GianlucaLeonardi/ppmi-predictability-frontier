# Exact-pinned dependencies for the PPMI predictability-frontier benchmark.
# Python 3.10 is required (statsmodels MixedLM + xgboost 3.x compatibility).
# These versions match the environment that produced the 2026-04-19 canonical run
# (5 x 5 x 3 x 15 nested CV; results/logs/run_all.log).

numpy==1.26.3
pandas==2.2.3
scipy==1.15.2
scikit-learn==1.6.1
xgboost==3.2.0
statsmodels==0.14.6
matplotlib==3.10.0
seaborn==0.13.2
shap==0.49.1
tabulate==0.9.0
jupyter==1.0.0
